import React from "react";

const Navbar = () => {
  return (
    <nav className="navbar">
      <a href="#">Home</a>
      <a href="#" className="active">Trainers</a>
    </nav>
  );
};

export default Navbar;